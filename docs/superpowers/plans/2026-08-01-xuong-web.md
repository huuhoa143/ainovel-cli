# Kế hoạch 3/4 — Xưởng (màn quản lý tác phẩm)

> **Cho người thi hành:** KỸ NĂNG BẮT BUỘC — dùng `superpowers:subagent-driven-development`.

**Mục tiêu:** một bề mặt trả lời "tôi đang có những gì, và đã tiêu bao nhiêu" — lỗ hổng thật
mà người dùng nêu (*"ngoài ra còn quản lý sản phẩm"*). Hiện tổng tiền cả xưởng không hiện ở
đâu cả.

**Kiến trúc:** như kế hoạch 2/4 — phép tổng hợp và luật đáp là hàm THUẦN ở `web/lib/`, component
chỉ vẽ. Dữ liệu đã có sẵn: `/workshop` mang `cost_usd`, `cost_per_chapter`, `chapters_per_hour`,
`updated_at`, `engine_open` cho từng cuốn (kế hoạch 1/4, đã đo trên cuốn thật).

**Nền:** nhánh `feat/viet-hoa-i18n` sau khi kế hoạch 2/4 hạ cánh.

---

## Luật cho người thi hành

Giống kế hoạch 2/4, lặp lại vì chúng đã trả giá để có:

1. **KHÔNG xóa chú thích đang có.** Sửa cho đúng nếu nó thành sai.
2. **Worktree đặt NGOÀI repo**, và `web/node_modules` phải là thư mục THẬT (Turbopack từ chối
   symlink trỏ ra ngoài gốc dự án — đã đo ở cụm A của kế hoạch 2/4).
3. **Cổng là DELTA so với nền**, chạy toàn bộ, không `-x`, không lọc gói.
4. **Nhãn phải qua `web/lib/nhan.ts`.**
5. **Bài kiểm phải đỏ trước VÌ ĐÚNG LÝ DO nó canh.** Và nghi ngờ chính bài kiểm: ở kế hoạch
   2/4, một bài kiểm do kế hoạch cung cấp KHÔNG đo được điều nó tự nói là đo.

---

## Quyết định đã chốt (từ spec §4, không bàn lại)

| # | Quyết định | Vì sao |
|---|---|---|
| 4 | Xưởng **KHÔNG có nút chạy**; dòng chỉ có `Mở` | một đường tiêu tiền duy nhất, ở transport. Hai nút cùng gọi `POST /run` thì trạng thái khóa-lúc-đang-gửi của chúng không thấy nhau → bấm cả hai là hai lượt chạy. Tệ hơn: tiêu tiền từ một màn người ta đang quét mắt |
| 8 | Xưởng **không xóa/đổi tên** tác phẩm | xóa một cuốn là xóa hàng giờ chạy và hàng chục đô; để ở hệ tệp nơi thấy rõ mình đang phá cái gì |

---

## Cấu trúc tệp

| Tệp | Việc | Trạng thái |
|---|---|---|
| `web/lib/xuong.ts` | tổng hợp cả xưởng + luật bề mặt đáp | mới |
| `web/components/Xuong.tsx` | dải tổng + bảng một dòng một cuốn | mới |
| `web/lib/khu.ts` | thêm khu `xuong` | sửa |
| `web/lib/useStudio.ts` | luật đáp lúc mở trang | sửa |
| `web/components/Rail.tsx` | mục Xưởng | sửa |
| `web/app/page.tsx` | định tuyến khu `xuong` | sửa |
| `web/lib/nhan.ts` | nhãn mới | sửa |
| `web/app/globals.css` | bảng + dải tổng + điểm gãy | sửa |

---

### Task 1: Tổng hợp cả xưởng

**Tệp:** tạo `web/lib/xuong.ts`, `web/lib/xuong.test.ts`

- [ ] **Bước 1: Viết bài kiểm đỏ**

```ts
// web/lib/xuong.test.ts
import { expect, test } from 'vitest';

import type { Book } from './types';
import { tongXuong } from './xuong';

function sach(p: Partial<Book>): Book {
  return {
    id: 'b', name: 'B', phase: 'writing', completed_chapters: 0, total_chapters: 0,
    total_words: 0, activity: 'idle', cost_usd: 0, cost_per_chapter: 0,
    chapters_per_hour: 0, engine_open: false,
    ...p,
  };
}

test('cộng chương đã chốt, số từ, chi phí và số engine đang mở', () => {
  const t = tongXuong([
    sach({ id: 'a', completed_chapters: 3, total_words: 5305, cost_usd: 1.67, engine_open: true }),
    sach({ id: 'b', completed_chapters: 2, total_words: 3000, cost_usd: 0.9 }),
  ]);

  expect(t.soTacPham).toBe(2);
  expect(t.chuongDaChot).toBe(5);
  expect(t.soTu).toBe(8305);
  expect(t.chiPhi).toBeCloseTo(2.57, 6);
  expect(t.engineDangMo).toBe(1);
});

test('xưởng rỗng cho mọi số bằng 0, không ném', () => {
  const t = tongXuong([]);
  expect(t.soTacPham).toBe(0);
  expect(t.chiPhi).toBe(0);
});

test('chi phí cộng bằng số nguyên xu, không tích lũy sai số dấu phẩy động', () => {
  // 0.1 + 0.2 !== 0.3 trong IEEE-754. Với mười cuốn thì sai số đủ để tổng hiện ra
  // $18,299999999999997 — một con số nói dối về độ chính xác nó có.
  const t = tongXuong([sach({ cost_usd: 0.1 }), sach({ cost_usd: 0.2 })]);
  expect(t.chiPhi).toBe(0.3);
});
```

- [ ] **Bước 2: Chạy → FAIL** (`cd web && npx vitest run lib/xuong.test.ts`)

- [ ] **Bước 3: Viết `xuong.ts`**

```ts
import type { Book } from './types';

/** Con số của cả xưởng, cho dải tổng ở đầu màn Xưởng. */
export interface TongXuong {
  soTacPham: number;
  chuongDaChot: number;
  soTu: number;
  /** Đô la. Cộng qua xu để không tích lũy sai số dấu phẩy động. */
  chiPhi: number;
  engineDangMo: number;
}

/**
 * Tổng hợp cả xưởng từ danh sách `/workshop`.
 *
 * Cộng chi phí bằng SỐ NGUYÊN XU rồi mới chia lại. `0.1 + 0.2` trong IEEE-754 là
 * `0.30000000000000004`, và với mười cuốn thì sai số đủ để dải tổng hiện
 * `$18,299999999999997` — một con số nói dối về độ chính xác mà nó có. Đây là dải trả lời câu
 * "tôi đã tiêu bao nhiêu", nên nó không được có chữ số vô nghĩa.
 *
 * Chỉ cộng, không suy: mọi trường vào đây đã do server tính trên cùng một `progress` và cùng
 * một danh sách checkpoint. Tính lại ở đây là nhân bản logic engine — đúng thứ PRODUCT.md cấm,
 * và nó sẽ lệch ngay lần engine đổi cách tính.
 */
export function tongXuong(sach: Book[]): TongXuong {
  let xu = 0;
  const t: TongXuong = {
    soTacPham: sach.length,
    chuongDaChot: 0,
    soTu: 0,
    chiPhi: 0,
    engineDangMo: 0,
  };
  for (const b of sach) {
    t.chuongDaChot += b.completed_chapters;
    t.soTu += b.total_words;
    xu += Math.round(b.cost_usd * 100);
    if (b.engine_open) t.engineDangMo += 1;
  }
  t.chiPhi = xu / 100;
  return t;
}
```

- [ ] **Bước 4: Chạy → 3 passed**

- [ ] **Bước 5: Phép thử đột biến**

| # | Đột biến | Phải đỏ ở |
|---|---|---|
| 1 | `xu += Math.round(b.cost_usd * 100)` → `t.chiPhi += b.cost_usd` | "cộng bằng số nguyên xu" |
| 2 | `if (b.engine_open) t.engineDangMo += 1` → `t.engineDangMo = sach.length` | "cộng … số engine đang mở" |

- [ ] **Bước 6: Commit**

---

### Task 2: Luật bề mặt đáp

Spec §7.1: có `?tp=` → buồng lái. Không có `?tp=` và **≥ 2** tác phẩm → Xưởng. Không có `?tp=`
và **đúng 1** tác phẩm → buồng lái của cuốn đó (một bảng một dòng không quyết định gì). Xưởng
rỗng → Tác phẩm mới (đã có, không đụng).

**Tệp:** sửa `web/lib/xuong.ts`, `web/lib/xuong.test.ts`

- [ ] **Bước 1: Viết bài kiểm đỏ**

```ts
import { khuDap } from './xuong';

test('có tp trên URL thì đáp thẳng vào buồng lái — người quay lại không phải bấm thêm nhịp', () => {
  expect(khuDap({ tpTuUrl: 'mac-the', khuTuUrl: undefined, soSach: 5 })).toBe('dong-san-xuat');
});

test('không có tp và xưởng nhiều cuốn thì đáp vào Xưởng', () => {
  expect(khuDap({ tpTuUrl: undefined, khuTuUrl: undefined, soSach: 3 })).toBe('xuong');
});

test('không có tp mà đúng MỘT cuốn thì đáp vào buồng lái, không phải bảng một dòng', () => {
  expect(khuDap({ tpTuUrl: undefined, khuTuUrl: undefined, soSach: 1 })).toBe('dong-san-xuat');
});

test('khu ghi rõ trên URL luôn THẮNG luật đáp', () => {
  // Tải lại trang ở bất kỳ màn nào phải về đúng màn đó. Luật đáp chỉ quyết định khi URL im.
  expect(khuDap({ tpTuUrl: undefined, khuTuUrl: 'chi-phi', soSach: 9 })).toBe('chi-phi');
  expect(khuDap({ tpTuUrl: 'mac-the', khuTuUrl: 'ban-thao', soSach: 9 })).toBe('ban-thao');
});
```

- [ ] **Bước 2: Chạy → FAIL**

- [ ] **Bước 3: Viết**

```ts
import { KHU_MAC_DINH, type Khu } from './khu';

/**
 * Bề mặt đáp lúc mở trang.
 *
 * Ba nhánh, ba lý do khác nhau:
 *
 *   - **URL nói rõ khu** → theo URL, không bàn. Tải lại trang ở một màn phải về đúng màn đó;
 *     một luật đáp thắng URL là URL nói dối.
 *   - **có `?tp=`** → buồng lái. Người quay lại một cuốn cụ thể đã nói ra họ muốn gì; bắt họ
 *     đi qua một bảng nữa là thêm một nhịp cho mọi lần mở trang.
 *   - **không `?tp=`** → Xưởng nếu có ≥ 2 cuốn, buồng lái nếu đúng 1. Một bảng một dòng không
 *     quyết định gì, nên nó chỉ là một màn hình phải bấm qua.
 *
 * Xưởng RỖNG không xử ở đây: `page.tsx` đã dẫn thẳng vào Tác phẩm mới trước mọi nhánh khác, và
 * lý do được ghi ở đó.
 */
export function khuDap(v: {
  tpTuUrl: string | undefined;
  khuTuUrl: Khu | undefined;
  soSach: number;
}): Khu {
  if (v.khuTuUrl) return v.khuTuUrl;
  if (v.tpTuUrl) return KHU_MAC_DINH;
  return v.soSach >= 2 ? 'xuong' : KHU_MAC_DINH;
}
```

- [ ] **Bước 4: Chạy → 7 passed**

- [ ] **Bước 5: Commit**

---

### Task 3: Khu `xuong` + mục trong rail

**Tệp:** `web/lib/khu.ts`, `web/components/Rail.tsx`, `web/lib/nhan.ts`

- [ ] Thêm `'xuong'` vào union `Khu` và vào mảng `KHU`.
- [ ] `laKhuMucMay('xuong')` phải trả **true**: Xưởng là bề mặt mức MÁY, không mức tác phẩm —
      nội dung của nó không đổi khi người dùng chuyển tác phẩm. Viết bài kiểm cho điều đó
      trong `web/lib/khu.test.ts` (tạo mới).
- [ ] `dungInspector('xuong')` phải trả **false** — bảng đã mang mọi thứ nó cần.
- [ ] Rail: đặt Xưởng ở **đầu** nhóm `Chung cho mọi tác phẩm`, TRÊN `Tác phẩm mới`. Lý do:
      thứ tự đó là thứ tự câu hỏi — "tôi có gì" đứng trước "thêm một cái nữa".
- [ ] Nhãn: `CHU.xuong = 'Xưởng'`, và các nhãn cột ở Task 4.

---

### Task 4: `Xuong.tsx` — dải tổng + bảng

**Tệp:** tạo `web/components/Xuong.tsx`, `web/components/Xuong.test.tsx`

Dải tổng: `N tác phẩm · N chương đã chốt · N từ · $N đã tiêu · N engine đang mở`.

Bảng, một dòng một cuốn, các cột: tên + mã · giai đoạn · tiến độ (số + thanh) · số từ · chi
phí (tổng + $/chương) · nhịp ch/giờ · sửa lần cuối · hành động.

- [ ] **Bài kiểm phải có, tối thiểu:**

1. dải tổng hiện đúng năm con số từ `tongXuong`
2. mỗi cuốn có đúng một dòng, và mã cuốn hiện ra (hai cuốn có thể trùng tên)
3. **`chapters_per_hour === 0` hiện dấu "chưa đo được", KHÔNG hiện `0 ch/giờ`** — một cuốn
   chưa chạy lần nào không có nhịp, và `0 ch/giờ` nói rằng nó chạy mà chạy rất chậm
4. `cost_per_chapter === 0` cùng luật đó
5. `updated_at` vắng → hiện `—`, không hiện `Invalid Date`
6. dòng có `engine_open` mang chữ "engine đang mở" và một lớp CSS riêng

- [ ] **Định dạng số dùng `web/lib/dinhdang.ts`** (`so`, `soTu`, `tienDo`) — đừng viết lại.

- [ ] Chú thích bắt buộc trong tệp: vì sao KHÔNG có nút chạy (quyết định 4 ở đầu kế hoạch này,
      chép nguyên lý do vào).

---

### Task 5: Hành động trên mỗi dòng

- [ ] Mọi cuốn: nút **`Mở`** → `onChonTacPham(id)` rồi về khu `dong-san-xuat`, ghi URL MỘT lần.
      Dùng đúng mẫu `moTacPhamVuaTao` trong `useStudio.ts` và ĐỌC chú thích của nó trước:
      gọi `chonTacPham` rồi `chonKhu` liên tiếp trong một handler sẽ ghi URL bằng cuốn CŨ, vì
      ref chỉ được đặt lại lúc render. Lỗi đó đã đo được hai lần trong dự án này.
- [ ] Cuốn `phase === 'complete'`: thêm `Đọc` (→ `docChuong(1)`) và `Xuất bản` (→ khu
      `nhap-xuat`).
- [ ] **KHÔNG có nút chạy, không có nút xóa, không có đổi tên.**
- [ ] Bài kiểm: (a) cuốn chưa xong chỉ có `Mở`; (b) cuốn đã xong có cả ba; (c) không tồn tại
      nút nào có nhãn chứa "Chạy" hay "Xóa" trên bề mặt này — viết khẳng định đó ra, nó là
      hàng rào chống một lần "tiện tay thêm nút" trong tương lai.

---

### Task 6: Đấu luật đáp vào `useStudio`

**Tệp:** `web/lib/useStudio.ts`

- [ ] `khu` hiện đọc từ URL lúc dựng state (`useState<Khu>(khuTuUrl)`). Luật đáp cần **số
      lượng sách**, mà số đó chỉ biết sau khi `/workshop` trả về. Nên: giữ nguyên `khuTuUrl`
      lúc dựng, và trong effect §1 — chỗ đã chọn `tacPham` — gọi `khuDap` và `setKhu` khi URL
      KHÔNG ghi khu.
- [ ] **Đừng ghi đè khu khi URL đã ghi rõ.** Bài kiểm của Task 2 đã khẳng định luật đó; chỗ
      đấu dây phải tôn trọng nó.
- [ ] `chonTacPham` từ Xưởng phải ghi URL một lần với cả ba mảnh (xem Task 5).
- [ ] Cổng: `npm test && npx tsc --noEmit && npm run build`.

---

### Task 7: CSS

- [ ] Bảng: `table-layout: fixed`, cột số căn phải, cột tên co giãn.
- [ ] Dòng `engine đang mở`: nền vàng nhạt, dùng token đang có. Một màu tín hiệu duy nhất
      (vàng) — luật 5 của `PRODUCT.md`. `--ink-3` là sàn cứng.
- [ ] `@media (max-width: 860px)`: bảng đổi sang thẻ xếp dọc, KHÔNG cuộn ngang.
- [ ] Kiểm: 0 tràn ngang ở 1440 và 390; tương phản AA cho mọi phần tử có chữ.

---

### Task 8: Cổng cuối + E2E trên xưởng THẬT

- [ ] `go build` · `go vet` · `gofmt -l` rỗng · `go test -count=1 ./...` → **30 gói / 0 FAIL**
- [ ] `cd web && npm test && npx tsc --noEmit && npm run build`
- [ ] E2E trên xưởng thật (không `seed-demo`):

| # | Việc | Bằng chứng |
|---|---|---|
| 1 | mở app không có `?tp=`, xưởng ≥ 2 cuốn | đáp vào Xưởng |
| 2 | tổng tiền ở dải | khớp tổng `cost_usd` của mọi cuốn, đối chiếu bằng `curl /api/workshop \| jq` |
| 3 | bấm `Mở` một cuốn | vào buồng lái đúng cuốn đó; URL có `?tp=` đúng |
| 4 | tải lại trang | vẫn ở buồng lái cuốn đó, không nhảy về Xưởng |
| 5 | vào `?khu=chi-phi` rồi tải lại | vẫn ở Chi phí — luật đáp không được thắng URL |
| 6 | xưởng đúng 1 cuốn | đáp vào buồng lái, không phải bảng một dòng |
| 7 | chụp 1440 + 390 | 0 tràn ngang |

- [ ] Kiểm `git merge-base` trước khi gộp `--ff-only`, và chạy LẠI toàn bộ cổng **trên cây đã
      gộp**. Cổng xanh trong worktree không chứng minh gì về cây đã gộp.

---

## Nhật ký thi hành

Người thi hành ghi vào đây: phép thử đột biến, lỗ hổng bộ kiểm, và mọi chỗ kế hoạch này SAI.

---

### Cụm Xưởng-logic (Task 1–5) — thi hành xong

**Commit** (nền = `4d17f7a`):

| sha | tiêu đề |
|---|---|
| `f8826fc` | feat(web): tổng hợp cả xưởng, chi phí cộng bằng số nguyên xu |
| `6df38f7` | feat(web): luật đáp lúc mở trang — Xưởng khi URL im và có từ hai cuốn |
| `9905f52` | feat(web): khu `xuong` là bề mặt mức máy, và mục Xưởng đầu nhóm chung |
| `46b5a86` | feat(web): màn Xưởng — dải tổng và bảng một dòng một cuốn |
| `aca2bfa` | feat(web): hành động trên mỗi dòng — Mở, và Đọc/Xuất bản cho cuốn đã xong |

#### HAI LẦN bộ đo của tôi không đo gì, và cả hai lần nó báo "xanh"

Ghi lên đầu vì đây là thứ đắt nhất cụm này học được, và nó lặp lại đúng bài học của cụm D
(bộ đo tương phản đọc `rgb()` bằng regex trong khi Chrome trả `oklch`, cho ra "0 vi phạm").

1. **Bảng đột biến đầu tiên của Task 1 chạy trên mã KHÔNG bị đột biến.** Hàm bọc truyền
   `"$@"` cho `perl -0pi -e '…'` mà quên chính TÊN TỆP, nên perl đọc stdin và tệp nguồn
   nguyên vẹn. Kết quả: **7/7 "XANH"** — tức một bảng nói bộ kiểm thủng hoàn toàn, trong khi
   thật ra chưa có đột biến nào xảy ra.
2. **Bảng đột biến đầu tiên của Task 4 chạy trên KHÔNG tệp kiểm nào.** Danh sách hai tệp để
   trong một biến rồi truyền không ngoặc — zsh KHÔNG tách từ như bash, nên vitest nhận đúng
   một đường dẫn chứa dấu cách, không khớp tệp nào, và dòng tổng kết vắng mặt. Kết quả:
   **17/17 dòng trống**.

Bịt bằng hai cái chốt, và mọi bảng dưới đây đều chạy qua chúng:

- sau khi đột biến, `diff` tệp với bản gốc — không khác thì in **"ĐỘT BIẾN KHÔNG ĂN"**;
- dòng tổng kết rỗng thì in **"PHÉP ĐO HỎNG"** thay vì để nó thành một ô trống trông như xanh.

Lần một tự lộ ra vì 7/7 xanh là điều không tin nổi; lần hai lộ vì ô trống. Cả hai đều có thể
đã trôi qua nếu con số hợp lý hơn một chút.

#### Task 1 — `tongXuong`

| # | Đột biến | Kết quả | Bài kiểm bắt được |
|---|---|---|---|
| KH-1 | `xu += Math.round(cost*100)` → cộng thẳng float | ĐỎ | "cộng bằng số nguyên xu" |
| KH-2 | `if (engine_open) …` → `= sach.length` | ĐỎ | "cộng … số engine đang mở" |
| R-3 | `Math.round` → `Math.trunc` | **XANH — LỖ HỔNG** | không bài nào |
| R-4 | bỏ cộng `completed_chapters` | ĐỎ | bài 1 |
| R-5 | bỏ cộng `total_words` | ĐỎ | bài 1 |
| R-6 | `soTacPham: sach.length` → `0` | ĐỎ | bài 1 |
| R-7 | quên chia lại 100 | ĐỎ | 2 bài |
| R-8 (sau khi bịt) | cộng số thực rồi mới làm tròn | ĐỎ | bài mới |

**Lỗ hổng R-3 — bốn giá trị chi phí của kế hoạch đều là bội của một xu, nên quyết định làm
tròn không được bài nào chạm tới.** `1.67 · 0.9 · 0.1 · 0.2` nhân 100 đều rơi đúng phía dưới
số nguyên, nên `round` và `trunc` cho CÙNG kết quả. Đây là biến thể thứ tư của lớp lỗi mà cụm
A (`'y'.repeat`), cụm B (`900 - LE_DAY + 1`) và cụm C (`[la 3]` mồ côi) đã gặp: đầu vào của
bài kiểm không nằm ở ca mà hằng số/quyết định đang canh có hiệu lực.

Bịt bằng chi phí THẬT đã đo trên hai cuốn của xưởng này — `2.1965540000000003` và
`7.906347000000001`, tức có độ phân giải dưới xu. Ba cách viết cho ba con số khác nhau:

| cách | tổng |
|---|---|
| làm tròn từng cuốn rồi cộng ← **cách đã chọn** | `$10,11` |
| cộng số thực rồi mới làm tròn | `$10,10` |
| cắt cụt từng cuốn | `$10,09` |

Và ở đây phát hiện được **lý do THẬT của quyết định "cộng bằng xu", khác với lý do kế hoạch
ghi.** Kế hoạch nói để tránh tích lũy sai số dấu phẩy động — nhưng với mười cuốn cỡ chục đô,
sai số tích lũy là ~1e-15, tức vô hại; thứ duy nhất hỏng là đuôi rác khi HIỂN THỊ, và một lần
làm tròn ở cuối cũng chữa được. Lý do thật nằm chỗ khác: **bảng ngay dưới dải tổng hiện chi
phí từng cuốn với hai chữ số thập phân** (`$2,20` và `$7,91`), nên một dải tổng ghi `$10,10`
đứng trên một cột cộng tay ra `$10,11` là một bảng không tự cộng được — trên đúng bề mặt tồn
tại để trả lời "tôi đã tiêu bao nhiêu". `$10,10` gần sự thật số học hơn nhưng nó là con số
sai ở đây. Đã ghi lý do này vào chú thích của `tongXuong`.

#### Task 2 — `khuDap` (kế hoạch không có bảng; bảng này của tôi)

| # | Đột biến | Kết quả | Bài kiểm bắt được |
|---|---|---|---|
| D-1 | `>= 2` → `> 2` | **XANH — LỖ HỔNG** | không bài nào |
| D-2 | `>= 2` → `>= 1` | ĐỎ | "đúng MỘT cuốn" |
| D-3 | `>= 2` → `!== 1` | **XANH — LỖ HỔNG** | không bài nào |
| D-4 | bỏ nhánh `tpTuUrl` | ĐỎ | "có tp trên URL" |
| D-5 | bỏ nhánh `khuTuUrl` | ĐỎ | 2 bài |
| D-6 | đảo thứ tự hai nhánh đầu | ĐỎ | "khu ghi rõ trên URL luôn THẮNG" |
| D-7 | URL im thì luôn trả `xuong` | ĐỎ | 2 bài |
| D-8 | `khuTuUrl` thắng nhưng trả mặc định | ĐỎ | "URL luôn THẮNG" |

**Lỗ hổng D-1 — bốn bài của kế hoạch dùng `soSach` = 5 · 3 · 1 · 9, không bài nào đứng ở
MỐC.** Spec §7.1 chốt ngưỡng là "≥ 2", và `> 2` thỏa cả bốn bài trong khi hệ quả là một xưởng
hai cuốn không bao giờ thấy được màn Xưởng — đúng ca nhỏ nhất mà một bảng bắt đầu có nghĩa.
**Lỗ hổng D-3** cùng loại ở đầu kia: không bài nào chạm `soSach === 0`.

Bịt bằng hai bài mới ở `soSach = 2` và `soSach = 0`.

**Thêm một bài kế hoạch không đòi, và nó canh cái mắt dễ tuột nhất của sợi dây này.**
`khuTuUrl()` trong `useStudio.ts` trả `KHU_MAC_DINH` khi query string KHÔNG có `?khu=`, còn
`ghiUrl` cố ý bỏ `khu` khỏi URL khi nó bằng `KHU_MAC_DINH` — nên hai ca "URL im" và "URL ghi
dong-san-xuat" đến đó **y hệt nhau**. Ai làm Task 6 mà truyền thẳng `khuTuUrl()` vào `khuDap`
sẽ giết cả luật đáp: mọi lần mở trang rơi vào nhánh đầu, không ai thấy màn Xưởng, và **không
bài kiểm nào của `khuDap` đỏ** vì hàm vẫn đúng. Bài mới chốt rằng `undefined` mới là "URL im";
chú thích của hàm nói thẳng người gọi phải đọc tham số THÔ.

#### Task 3 — khu + rail (kế hoạch không có bảng; bảng này của tôi)

| # | Đột biến | Kết quả | Bài kiểm bắt được |
|---|---|---|---|
| K-1 | bỏ `'xuong'` khỏi `laKhuMucMay` | ĐỎ | "Xưởng là khu mức MÁY" |
| K-2 | `dungInspector` nhận cả `xuong` | ĐỎ | "Xưởng KHÔNG dùng inspector" |
| K-3 | `dungInspector` luôn `false` | ĐỎ | cùng bài (vế ngược) |
| K-4 | `laKhuMucMay` luôn `true` | ĐỎ | cùng bài (vế ngược) |
| K-5 | bỏ `'xuong'` khỏi mảng `KHU` | ĐỎ | 2 bài |
| K-6 | nhân đôi một mục trong `KHU` | ĐỎ | "không thiếu không trùng" |
| R-1 | bỏ hẳn mục Xưởng khỏi rail | ĐỎ | 3 bài |
| R-2 | mục Xưởng trỏ nhầm khu | ĐỎ | 2 bài |
| R-3 | Xưởng xuống cuối nhóm chung | ĐỎ | "đứng ĐẦU nhóm chung" |

Không lỗ hổng. Nói thẳng một chuyện: **ba trong bốn bài của `khu.test.ts` XANH ngay lần chạy
đầu** (chỉ `laKhuMucMay` đỏ). Hai bài về mảng `KHU` xanh vì Task 2 đã phải thêm `'xuong'` vào
đó để biên dịch; bài `dungInspector` xanh vì mặc định của hàm đã là `false`. Xanh sẵn không
có nghĩa là thừa — K-2/K-3/K-5/K-6 cho thấy chúng bắt được thứ thật — nhưng nó có nghĩa là
chúng KHÔNG được viết theo lối đỏ-trước, và bảng này là thứ duy nhất chứng minh chúng có giá.

**Hai bộ kiểm thêm ngoài kế hoạch:**

1. **`Record<Khu, true>` trong `khu.test.ts`** — chốt duy nhất bắt được ca "thêm khu vào
   union mà quên mảng `KHU`". Hệ quả của ca đó im lặng hoàn toàn: `laKhu` từ chối mã khu, nên
   điều hướng bằng rail vẫn chạy nhưng TẢI LẠI TRANG ở khu mới lặng lẽ về khu mặc định — URL
   thôi giữ đúng chỗ đang xem, không lỗi nào nổ ra.
2. **`components/Rail.test.tsx`** — repo chưa từng có bộ kiểm cho `Rail`. Mục rail là đường
   vào DUY NHẤT của bề mặt Xưởng ngoài `?khu=xuong` gõ tay, mà bộ kiểm của `Xuong` dựng
   component thẳng, nên xóa hẳn mục rail vẫn xanh cả bộ (đột biến R-1 xác nhận trước khi có
   tệp này). Cùng lớp "mắt cuối của sợi dây" mà cụm D đo được với `vanSong={s.vanSong}`.

#### Task 4 — `Xuong.tsx` (kế hoạch không có bảng; bảng này của tôi)

| # | Đột biến | Kết quả | Bài kiểm bắt được |
|---|---|---|---|
| X-1 | nhịp: luôn hiện số, kể cả 0 | ĐỎ | "nhịp: chưa chạy để TRỐNG" |
| X-2 | nhịp: luôn để trống | ĐỎ | cùng bài (vế ngược) |
| X-3 | giá thành: luôn hiện số | ĐỎ | "giá thành: 0 là chưa đo được" |
| X-4 | giá thành: luôn để trống | ĐỎ | cùng bài (vế ngược) |
| X-5 | tổng chi phí gộp vào "chưa đo được" | ĐỎ | "$0,00 là một phép đo" |
| X-6 | bỏ mã cuốn khi đã có tên | ĐỎ | "hai cuốn trùng tên" |
| X-7 | bỏ lớp `engmo` trên dòng | ĐỎ | "dòng engine đang mở" |
| X-8 | mọi dòng đều mang huy hiệu engine | ĐỎ | 2 bài |
| X-9 | thanh tiến độ vẽ cả khi chưa biết tổng | ĐỎ | "thanh KHÔNG vẽ khi chưa biết tổng" |
| X-10 | bỏ hẳn thanh tiến độ | ĐỎ | cùng bài (vế ngược) |
| X-11 | dấu trống không nói vì sao | ĐỎ | 2 bài |
| X-12 | bỏ câu vì-sao-không-có-nút-chạy | ĐỎ | "bề mặt nói ra vì sao" |
| X-13 | dải tổng bỏ ô engine đang mở | ĐỎ | "dải tổng hiện năm con số" |
| X-14 | dải tổng in chi phí thô | ĐỎ | cùng bài |
| X-15 | bảng chỉ vẽ cuốn đầu | ĐỎ | 6 bài |
| P-1 | khu `xuong` rơi về buồng lái | ĐỎ | 2 bài |
| P-2 | `Trang` truyền mảng rỗng xuống `Khu` | ĐỎ | "nối danh sách sách THẬT" |

Không lỗ hổng — và đó là kết quả của việc viết bộ kiểm theo đúng cảnh báo của bảng đột biến
cụm B **trước khi** chạy bảng: mỗi bài "chưa đo được" dựng HAI cuốn (một chưa đo được, một có
số thật) và đòi cả hai chiều cùng lúc. Không có vế ngược đó thì X-2, X-4 và X-10 — tức "vẽ
`—` cho MỌI cuốn", một bề mặt giấu sạch những con số nó tồn tại để hiện — đều xanh.

#### Task 5 — hành động (kế hoạch không có bảng; bảng này của tôi)

| # | Đột biến | Kết quả | Bài kiểm bắt được |
|---|---|---|---|
| H-1 | Đọc/Xuất bản hiện ở MỌI cuốn | ĐỎ | "cuốn chưa xong chỉ có Mở" |
| H-2 | bỏ hẳn Đọc và Xuất bản | ĐỎ | "cuốn đã hoàn thành có đủ ba" |
| H-3 | `Mở` dùng mã cuốn cứng | ĐỎ | "đúng cuốn đó" |
| H-4 | `Đọc` quên truyền chương 1 | ĐỎ | "chương 1 đi CÙNG lời gọi" |
| H-5 | `Đọc` đi nhầm khu | ĐỎ | cùng bài |
| H-6 | `Xuất bản` đi nhầm khu | ĐỎ | cùng bài |
| H-7 | thêm một nút "Chạy" vào dòng | ĐỎ | 3 bài |
| H-8 | thêm một nút "Xóa" vào dòng | ĐỎ | 3 bài |
| C-1 | cùng cuốn cũng coi là "đổi cuốn" | ĐỎ | 2 bài |
| C-2 | cùng cuốn LUÔN nạp lại | ĐỎ | "chỉ đổi khu, không gọi mạng" |
| C-3 | cùng cuốn KHÔNG bao giờ nạp lại | ĐỎ | "nạp lại tại chỗ" |
| C-4 | so sánh đảo chiều | ĐỎ | 3 bài |
| P-3 | `page.tsx` no-op hóa `onMoTacPham` | **XANH — LỖ HỔNG** | không bài nào |

**Lỗ hổng P-3 — mắt cuối của sợi dây, lần thứ ba trong dự án này.** Bộ kiểm của `Xuong`
truyền spy CỦA CHÍNH NÓ vào `onMoTacPham`, nên nó chỉ chứng minh component gọi đúng tham số —
không chứng minh nó được nối vào hành động thật. Thay `onMoTacPham={s.moTacPhamTai}` bằng một
hàm rỗng thì **34/34 vẫn xanh**, trong khi hệ quả là mọi nút trên bảng bấm vào không đi đâu
cả và không lỗi nào nổ ra. Bịt bằng một bài dựng cả `Trang` (`useStudio` bị thay, hành động
là `vi.fn()`) rồi bấm nút thật. Chạy lại P-3 và một biến thể tinh vi hơn (nối vào
`chonTacPham` thay vì hàm rỗng) → cả hai ĐỎ.

#### LỖI LÀM TREO MÀN HÌNH mà kế hoạch không lường

**Task 5 bảo dùng `docChuong(1)` cho nút `Đọc`. Làm đúng thế là hỏng, theo hai đường.**

`docChuong` và `chonKhu` đọc `tacPhamRef.current`, tức cuốn ĐANG XEM — không phải cuốn của
dòng được bấm. Bảng Xưởng liệt kê mọi cuốn, nên bấm `Đọc` trên dòng của cuốn khác sẽ mở
chương 1 của **cuốn đang xem**. Kế hoạch đã cảnh báo đúng cái bẫy ref-trễ này cho nút `Mở`
nhưng lại kê `docChuong(1)` cho `Đọc` ở ngay dòng dưới.

Và cái thứ hai nặng hơn, không có trong kế hoạch: **làm theo mẫu `moTacPhamVuaTao` cho ca
"mở đúng cuốn đang xem" sẽ TREO màn hình.** Mẫu đó xóa `snapshot` rồi trông cậy effect §2 nạp
lại — nhưng effect đó phụ thuộc `tacPham`, và đặt lại CÙNG một giá trị không làm React chạy
lại nó. Kết quả: `snapshot === undefined` vĩnh viễn và `page.tsx` đứng mãi ở màn "đang đọc
store…". `moTacPhamVuaTao` không bao giờ gặp ca đó vì cuốn vừa tạo luôn là cuốn mới; **bảng
Xưởng gặp nó ngay cú bấm đầu tiên**, vì cuốn đang mở LUÔN có một dòng trong bảng và bấm `Mở`
trên chính dòng đó là cú bấm tự nhiên nhất.

Đã xử: thêm `moTacPhamTai(id, khu, chuong?)` vào `useStudio` ghi cả ba mảnh URL một lần, và
`moTacPhamVuaTao` trở thành ca riêng của nó. Ba nhánh quyết định tách thành hàm THUẦN
`cachMoTacPham` ở `lib/xuong.ts` để ca treo có phép đo — nằm trong hook thì nó chỉ đo được
bằng cách giả lập cả `fetch` lẫn `EventSource`, tức bằng một bài kiểm đo nhầm thứ.

**Task 6 lưu ý:** kế hoạch nói "`chonTacPham` từ Xưởng phải ghi URL một lần với cả ba mảnh
(xem Task 5)" — việc đó ĐÃ XONG dưới tên `moTacPhamTai`, và `useStudio.ts` đã bị sửa ở cụm
này (thêm một hành động, một import). Không đụng tới `khu` state hay effect §1.

#### Chỗ kế hoạch sai hoặc thiếu

1. **Task 2 không dựng được nếu chưa có `'xuong'` trong union `Khu`, mà kế hoạch xếp
   `khu.ts` vào Task 3.** Đã đưa union + mảng `KHU` vào commit Task 2 (điều kiện biên dịch),
   để lại ngữ nghĩa (`laKhuMucMay`, `dungInspector`) cho Task 3 với bài kiểm đỏ-trước.
2. **Bảng tệp xếp `web/app/page.tsx` vào kế hoạch nhưng KHÔNG gắn nó vào Task nào.** Task 6
   chỉ nói `useStudio`. Đã đưa định tuyến vào commit Task 4: sau commit Task 3, mục rail
   Xưởng rơi vào `default` của `switch`, tức bấm vào nó ra buồng lái — một mục rail nói dối
   còn tệ hơn một bề mặt chưa có CSS.
3. **Task 3 bảo `laKhuMucMay('xuong')` phải trả `true`, nhưng hàm đó hôm nay KHÔNG CÓ NGƯỜI
   GỌI.** Đã đo: `grep laKhuMucMay lib components app` chỉ còn một lời viện dẫn trong chú
   thích ở `nhan.ts`. Vẫn thi hành theo kế hoạch và giữ hàm — cùng cách cụm D xử `DangLam`:
   "không tới được" là tính chất của người gọi hôm nay, không phải của thứ được gọi — nhưng
   ghi thẳng chuyện đó vào chú thích của hàm để người sau không tưởng nó đang gác cái gì.
4. **Task 5 kê `docChuong(1)` cho nút `Đọc`** — sai, xem mục "lỗi làm treo màn hình" ở trên.
5. **Task 4 liệt "hành động" trong danh sách cột nhưng mọi bài kiểm của nút nằm ở Task 5.**
   Đã tách: commit Task 4 dựng bảy cột đầu, commit Task 5 thêm cột hành động cùng các nút.
6. **Kế hoạch bảo `Định dạng số dùng dinhdang.ts (so, soTu, tienDo)` — thiếu bốn hàm mà bảng
   này thật sự cần:** `tongTien` (chi phí tổng), `donGia` (giá thành mỗi chương, ba chữ số
   thập phân), `nangSuat` (nhịp), `ngayGio` (sửa lần cuối). Cả bốn đã có sẵn; `ngayGio` cũng
   chính là thứ xử luôn yêu cầu "không hiện Invalid Date" — nó trả `undefined` cho mốc hỏng.
7. **Nhãn Xưởng KHÔNG chỉ có `CHU.xuong`.** Task 3 ghi "và các nhãn cột ở Task 4" nhưng
   không kê; thực tế cần 4 nhãn đơn vị dải tổng, 6 nhãn cột, 3 nhãn nút và 7 câu `GIAI_THICH`
   (trong đó ba câu "chưa đo được", hai câu chép nguyên lý do của quyết định 4 và 8).
8. **`CHU.xuatBan` đã có sẵn** (khối Nhập & Xuất). Dùng lại, không khai khóa mới — khai trùng
   khóa trong một object literal là JavaScript hợp lệ (bản sau thắng) nên `npm test` sẽ xanh
   trong khi hai nhãn cùng tên sống song song; thứ bắt được là `tsc`, đúng như cụm C đã ghi.

#### Quyết định tự đưa ra vì kế hoạch không nói

- **Fixture `sach()` đặt ở `components/mau.test-helper.ts`, không viết lại trong từng tệp
  kiểm** như kế hoạch phác ở Task 1. `lib/xuong.test.ts` và `components/Xuong.test.tsx` cùng
  dựng `Book`; hai bản sao thì lệch ngày hợp đồng `/workshop` thêm trường bắt buộc. Tệp đó
  chạy được ở cả hai project của vitest vì nó chỉ nhập kiểu. `snap()` giờ dựng `book` bằng
  chính `sach()` — cùng giá trị như trước, không bài nào đổi kết quả.
- **Mặc định của `sach()` là cuốn CHƯA CHẠY LẦN NÀO** (`chapters_per_hour: 0`,
  `cost_per_chapter: 0`), cùng lý do như năm trường sống `null` của `snap()`: bài kiểm nào
  cần ca "đo được" phải nói ra, nên không bài nào lỡ đo ca dễ mà tưởng mình đo ca khó.
- **`total_chapters === 0` thì KHÔNG vẽ thanh tiến độ.** Đó là "chưa biết tổng", không phải
  "tổng bằng không"; một thanh rỗng nói cuốn chưa đi được đoạn nào, một thanh đầy nói đã
  xong — cả hai đều là khẳng định dữ liệu không đưa ra. Con số `3` vẫn hiện.
- **`cost_usd === 0` in ra `$0,00`, KHÔNG gộp vào "chưa đo được".** Đó là một phép đo có thật
  (chưa tốn gì). Ranh giới này có bài kiểm riêng vì nó ngược chiều với hai ô ngay cạnh.
- **Ô "số từ" là ô trống DUY NHẤT không có câu giải thích.** "Chưa viết chữ nào" đọc thẳng ra
  được từ cột tiến độ ngay cạnh; ba ô kia thì không.
- **Mã cuốn hiện LUÔN, kể cả khi cuốn đã có tên** — chép nguyên luật và lý do của
  `ThanhTren.tsx`: hai cuốn ở hai thư mục khác nhau có thể trùng tên, và `id` là thứ duy nhất
  phân biệt được chúng.
- **`data-ma={b.id}` trên mỗi `<tr>`** để bài kiểm tra đúng dòng của một cuốn. `nth-child`
  trôi ngay lần thứ tự bảng đổi, và lúc đó bài kiểm đo nhầm dòng mà vẫn xanh.
- **Hai câu `GIAI_THICH` hiện RA MÀN HÌNH dưới bảng**, không nằm trong chú thích mã: người
  vận hành nhìn một bảng liệt kê mọi cuốn sẽ đi tìm nút chạy, và không tìm thấy mà không có
  lời giải thích là một khoảng lặng họ tự lấp bằng phỏng đoán.
- **Bài kiểm "không có nút Chạy/Xóa" quét cả `Dừng` và `Đổi tên`**, và dựng cả cuốn đang chạy
  lẫn cuốn đã xong — một nút thêm vào sau này nhiều khả năng chỉ hiện ở một trong hai ca.
- **Nút hành động dùng `.nutPhu` sẵn có, không khai một lớp nút mới.** Bản đầu tôi đặt
  `.nutnho` — một lớp không có một dòng CSS nào, tức ba nút render trần. `.nutChinh`/`.nutPhu`
  đã có đủ token (`globals.css:4415`); ba nút này không khác gì nút phụ ở bề mặt khác, và một
  lớp mới là một bộ token thứ hai phải giữ cho khớp.
- **KHÔNG đụng `app/globals.css`** — Task 7 sở hữu nó. **Mười bảy** lớp của bề mặt này chưa
  có một dòng CSS nào (đã đo bằng `grep` từng lớp): `.khuxuong .xtong .o .bangxuong .engmo
  .engnhan .phase .tiendo .thanh .day .tien .tong .donGia .nhip .sua .lam` — cộng `.day` bên
  trong `.thanh`. Bảng KHÔNG hoàn toàn không hình: `.canvas .head .bangwrap .bang .num .ten
  .nb .ma .sl .tu .trong .steerhint .nutPhu` đều đã có, nên khung bảng, đầu cột và nút đã
  đúng dạng. Thứ chưa có hình là **dải tổng, thanh tiến độ, nền vàng của dòng engine đang mở
  và huy hiệu của nó** — tức đúng bốn thứ Task 7 kê. Đúng theo bảng tệp, và cũng để tránh va
  chạm với phiên đang chạy song song.
- **KHÔNG đụng luật đáp trong `useStudio`** — Task 6 sở hữu nó. `khuDap` đã có nhưng chưa
  ai gọi; mục 4 của bảng đột biến Task 2 và bài "URL ghi rõ đúng khu mặc định" là thứ Task 6
  phải đọc trước khi đấu dây.

#### Cổng sau Task 5 (chạy toàn bộ trong worktree, không lọc gói, không `-x`)

`go build ./...` exit 0 · `go vet ./...` exit 0 · `gofmt -l .` **rỗng** ·
`go test -count=1 ./...` **30 gói ok / 0 FAIL** (đúng nền) ·
`npm test` **132/132** (nền 95/95, không bài nào đỏ) · `npx tsc --noEmit` exit 0 ·
`npm run build` exit 0.

`git merge-base --is-ancestor 4d17f7a HEAD` → đúng; cây làm việc sạch.
